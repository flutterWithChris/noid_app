import 'package:auto_route/annotations.dart';
import 'package:flutter/material.dart';

class ShopPage extends StatelessWidget {
  const ShopPage({Key? key, @PathParam(),}) : super(key: key);

  

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}